_: {
  nixavell = {
    features = {
      desktop = true;
      hyprde = true;
      laptop = true;
      programs = true;
      wsl = false;
    };

    home = { };

    programs = { };

    neovim = { };

    nixos = { };

    services = { };

    stateVersion = "25.05";

    users = { };
  };
}
