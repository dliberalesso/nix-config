_: {
  nixavell = {
    features = {
      desktop = true;
      hyprde = true;
      laptop = true;
      programs = true;
    };

    home = { };

    programs = { };

    neovim = { };

    nixos = { };

    services = { };

    system = "x86_64-linux";

    stateVersion = "25.05";

    users = { };
  };
}
