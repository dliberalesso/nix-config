_: {
  nixwsl = {
    features = {
      wsl = true;
    };

    home = { };

    programs = { };

    neovim = { };

    nixos = { };

    services = { };

    system = "x86_64-linux";

    stateVersion = "25.05";

    users = { };
  };
}
